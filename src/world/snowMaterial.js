import * as THREE from 'three';
// OWNER: agent "snow-shading". Placeholder until replaced.
export function createSnowMaterial() {
  return new THREE.MeshStandardMaterial({ color: 0xf2f6ff, roughness: 0.75, metalness: 0 });
}
export function updateSnowMaterial() {}
